// @erp/database — Supabase migrations, money functions, RLS, seeds, generated types.
// The generated DB types are the single source of truth (Article XI); no hand-written types.
export type { Database } from './types/database.js';
